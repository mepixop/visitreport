export enum Status {
  OPEN,
  IN_PROGRESS,
  CLOSED,
}

export enum VisitReportType {
  VIDEO_CALL,
  EXTERNAL_VISIT,
  INTERNAL_VISIT,
  EXPO_DISPLAY,
}

export enum VisitReportOutcome {
  NEGATIVE,
  NEUTRAL,
  POSITIVE,
  VERY_POSITIVE,
}
